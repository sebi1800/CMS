export const EMULATOR_API = "10.0.2.2";

export const config = {
  server: {
    url: `https://cmstwo.cyclic.app`,
  },
};

export const API_URL = `${config.server.url}/api/v1`;
export const API_URL2 = `${config.server.url}`;
